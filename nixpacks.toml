[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r epiweather-api/requirements.txt"]

[start]
cmd = "uvicorn main:app --app-dir epiweather-api --host 0.0.0.0 --port $PORT"
