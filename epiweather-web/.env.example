# epiweather-web 환경변수 예시
# .env.local로 복사해서 사용.

# 배포된 백엔드(epiweather-api) URL. 비우면 http://localhost:8000으로 폴백.
NEXT_PUBLIC_API_URL=

# 백엔드의 EPIWEATHER_API_KEY와 동일한 값.
# 빌드타임에 번들에 포함되는 공개값이라 관리자 비밀키가 아니라
# "신뢰 클라이언트(이 프론트엔드) vs 그 외" 최소 게이트 용도.
NEXT_PUBLIC_API_KEY=
